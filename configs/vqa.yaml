experiment_name: "vqa-run"
model: "Qwen/Qwen3.5-0.8B-Base"
cache_dir: "huggingface"
max_train_samples: null

vqa:
  add_prefix: false
  checkpoint: null
  map_writer_batch_size: 500

training:
  per_device_train_batch_size: 4
  per_device_eval_batch_size: 3
  fp16: false
  max_length: 512
  report_to: "none"
